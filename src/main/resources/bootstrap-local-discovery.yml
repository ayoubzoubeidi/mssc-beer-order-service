spring:
  cloud:
    discovery:
      enabled: true
    config:
      discovery:
        enabled: true
        service-id: brewery-config
      username: hello
      password: password

eureka:
  client:
    service-url:
      defaultZone: http://netflix:eureka@localhost:8761/eureka